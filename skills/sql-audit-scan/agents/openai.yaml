interface:
  display_name: "SQL Audit Scan"
  short_description: "Run local SQL audit scans for MyBatis and .sql inputs"
  default_prompt: "Use $sql-audit-scan to scan this local MyBatis directory, SQL file, or pasted SQL and summarize the results."
policy:
  allow_implicit_invocation: true
